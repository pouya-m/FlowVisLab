compile:
%%bash
python /content/setup.py build_ext --inplace

clone:
%%bash
git clone https://github.com/pouya-m/openpiv-python

get specific directory from github:
githubdl.dl_dir()

set and get PATH:
export PATH=$PATH:/place/with/the/file
echo $PATH
